# 如何像量化台一样模拟：完整预测市场模拟指南

- 原始链接：<https://x.com/gemchange_ltd/status/2027744530124951831>
- 作者：gemchanger（@gemchange_ltd）
- 发布时间：2026-02-28
- X Article：<http://x.com/i/article/2027371960175386624>

---

![](https://pbs.twimg.com/media/HCP-EHtbYAACOLP.jpg)

## 引言

这不是一份技术清单。这是一个故事——从抛硬币开始，最终到达机构级的模拟引擎。每个部分都以前一部分为基础。按顺序读完，你会拥有可运行的代码。

## 第一部分：那个打破常规的抛硬币

你正盯着 Polymarket 合约："美联储会在三月降息吗？" YES 正在以 $0.62 交易。你的直觉是：这意味着 62% 的概率。也许你觉得应该是 70%。于是你买了。

但你完全不知道：
- 对你的 70% 估计有多确定
- 明天的就业报告发布后，这个概率该如何变化
- 它与 Polymarket 上其他六个美联储相关合约的相关性是多少

一个抛硬币只有一个参数：p。一个预测市场合约有几十个参数。

## 第二部分：蒙特卡洛基础

事件概率 p = P(A) 的估计量就是样本均值。方差为 Var(p̂_N) = p(1−p)/N。

要在 p = 0.5 时达到 ±0.01 的精度（95% 置信度）：N = 9,604

### 你的第一个可运行模拟

```python
import numpy as np

def simulate_binary_contract(S0, K, mu, sigma, T, N_paths=100_000):
    Z = np.random.standard_normal(N_paths)
    S_T = S0 * np.exp((mu - 0.5 * sigma**2) * T + sigma * np.sqrt(T) * Z)
    payoffs = (S_T > K).astype(float)
    p_hat = payoffs.mean()
    se = np.sqrt(p_hat * (1 - p_hat) / N_paths)
    return {'probability': p_hat, 'std_error': se}
```

## 第三部分：重要性抽样

Polymarket 托管着极端事件合约。"标准普尔 500 会在一周内下跌 20%？"正在以 $0.003 交易。用 10 万次样本的简单蒙特卡洛，你的估计要么是 0.00000，要么是 0.00001——两者都没用。

### 尾部风险合约的重要性抽样

```python
def rare_event_IS(S0, K_crash, sigma, T, N_paths=100_000):
    K = S0 * (1 - K_crash)
    mu_original = -0.5 * sigma**2
    log_threshold = np.log(K / S0)
    mu_tilt = log_threshold / T
    Z = np.random.standard_normal(N_paths)
    log_returns_tilted = mu_tilt * T + sigma * np.sqrt(T) * Z
    S_T_tilted = S0 * np.exp(log_returns_tilted)
    log_returns_original = mu_original * T + sigma * np.sqrt(T) * Z
    log_LR = (-0.5 * ((log_returns_tilted - mu_original * T) / (sigma * np.sqrt(T)))**2
               + 0.5 * ((log_returns_tilted - mu_tilt * T) / (sigma * np.sqrt(T)))**2)
    LR = np.exp(log_LR)
    payoffs = (S_T_tilted < K).astype(float)
    is_estimates = payoffs * LR
    return is_estimates.mean(), is_estimates.std() / np.sqrt(N_paths)
```

IS 可以将方差降低 100–10,000 倍。

## 第四部分：粒子滤波器

想象选举之夜。美国东部时间晚上 8:01。佛罗里达刚结束投票。你的模型需要即时更新——将这个新数据点纳入概率估计。

```python
import numpy as np
from scipy.special import expit, logit

class PredictionMarketParticleFilter:
    def __init__(self, N_particles=5000, prior_prob=0.5,
                 process_vol=0.05, obs_noise=0.03):
        self.N = N_particles
        self.process_vol = process_vol
        self.obs_noise = obs_noise
        logit_prior = logit(prior_prob)
        self.logit_particles = logit_prior + np.random.normal(0, 0.5, N_particles)
        self.weights = np.ones(N_particles) / N_particles
    
    def update(self, observed_price):
        noise = np.random.normal(0, self.process_vol, self.N)
        self.logit_particles += noise
        prob_particles = expit(self.logit_particles)
        log_likelihood = -0.5 * ((observed_price - prob_particles) / self.obs_noise)**2
        log_weights = np.log(self.weights + 1e-300) + log_likelihood
        log_weights -= log_weights.max()
        self.weights = np.exp(log_weights)
        self.weights /= self.weights.sum()
        ess = 1.0 / np.sum(self.weights**2)
        if ess < self.N / 2:
            self._systematic_resample()
    
    def _systematic_resample(self):
        cumsum = np.cumsum(self.weights)
        u = (np.arange(self.N) + np.random.uniform()) / self.N
        indices = np.searchsorted(cumsum, u)
        self.logit_particles = self.logit_particles[indices]
        self.weights = np.ones(self.N) / self.N
    
    def estimate(self):
        probs = expit(self.logit_particles)
        return np.average(probs, weights=self.weights)
```

## 第五部分：方差缩减技巧

### 对偶变量、控制变量、分层抽样

这些技术可以叠加，实现 100–500 倍的方差缩减。

## 第六部分：Copula 与尾部依赖

**高斯 Copula**：尾部依赖 λ = 0。对预测市场来说是灾难性的错误。

**Student-t Copula**（ν=4, ρ=0.6）：尾部依赖约为 0.18。

```python
import numpy as np
from scipy.stats import norm, t as t_dist

def simulate_correlated_outcomes_t(probs, corr_matrix, nu=4, N=100_000):
    d = len(probs)
    L = np.linalg.cholesky(corr_matrix)
    Z = np.random.standard_normal((N, d))
    X = Z @ L.T
    S = np.random.chisquare(nu, N) / nu
    T = X / np.sqrt(S[:, None])
    U = t_dist.cdf(T, nu)
    outcomes = (U < np.array(probs)).astype(int)
    return outcomes
```

**Vine Copula**：对于 d > 5 个合约，将 d 维依赖分解为 d(d−1)/2 个二元条件 Copula。

## 第七部分：基于代理的模型

市场即使在每个交易者都完全非理性时也能有效。Gode & Sunder (1993) 表明，零智能代理——仅受预算约束的随机订单交易者——在连续双向拍卖中实现了接近 100% 的配置效率。

## 第八部分：生产级技术栈

**第一层：数据摄取**
- Polymarket CLOB API WebSocket 流
- 新闻/民调数据流（NLP 处理成概率信号）
- 链上事件数据

**第二层：概率引擎**
- 分层贝叶斯模型（Stan/PyMC）
- 粒子滤波器实时更新
- 跳跃扩散 SDE 路径模拟
- 集成：模型输出的加权平均

**第三层：依赖建模**
- Vine Copula 合约间两两依赖
- 因子模型共享全国/全球风险因子
- 通过 t-Copula 估计尾部依赖

**第四层：风险管理**
- 基于 EVT 的 VaR 和预期亏空
- 反向压力测试
- 相关性压力测试
- 流动性风险监测

**第五层：监测**
- Brier 分数跟踪
- P&L 归因
- 回撤预警
- 模型漂移检测

## 参考文献

- Dalen (2025). "Toward Black-Scholes for Prediction Markets."
- Saguillo et al. (2025). "Unravelling the Probabilistic Forest: Arbitrage in Prediction Markets."
- Madrigal-Cianci et al. (2026). "Prediction Markets as Bayesian Inverse Problems."
- Farmer, Patelli & Zovko (2005). "The Predictive Power of Zero Intelligence."
- Gode & Sunder (1993). "Allocative Efficiency of Markets with Zero-Intelligence Traders."
- Kyle (1985). "Continuous Auctions and Insider Trading."
- Glosten & Milgrom (1985). "Bid, Ask, and Transaction Prices."
